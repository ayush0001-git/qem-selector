# Fleet state recovered from old journal (60 agent results)

## Integrator
tests=True smoke=True boundaryYamlHours=5.2
notes: INTEGRATOR V2 COMPLETE. (1) FULL SUITE: 737 passed / 0 failed (pytest tests -q, 1776.8s under load; identical tree passed in 445s idle). The builders' lone red (test_shipped_hw_first_run_config_fits_two_minutes) was already root-cause-fixed: configs\hw_first_run.yaml reverted to hardware_confirmed:false with revert comment; test passes unweakened. No src/tests changes made by integration. (2) FRESH E2E results\boundary_smoke (I added FakeManilaV2@x0.5 to configs\boundary_smoke.yaml so both dial-down points are exercised; 18->24 units, no test pins it): run_experiment 24/24 in 104s; base_shots int column between pauli/ideal; all 7 techniques present; 15 feat_* cols incl feat_log2_shots; dial-down raw error LOWER than x1.0 siblings (mean 0.046@x0.25 < 0.067@x0.5 < 0.103@x1.0; 5/6 rows indivi

## Module testers: 8 results
- passed=True blocking=0 | T-backends round 1: PASS with zero findings (167 executed checks + full tiny.yaml regression rerun). (1) MONOTONE DIAL-DOWN: 60-CX identity circuit, ZZ, 32768 shots, seeds {0,1,2}, on FakeManilaV2 AND
- passed=True blocking=0 | ADVERSARIAL TESTER T-stats round 1: PASS — 88/88 checks green by execution (scripts in scratchpad: t_stats_adv.py 78 checks, t_stats_adv2.py 10 checks; run with "E:\\quatum  computiiing\\qem-selector\
- passed=True blocking=0 | T-features round 1: PASS, zero critical/major. All verification by running code (harness: scratchpad\adv_features_t1.py; note at E:\quatum  computiiing\qem-selector\notes\tester-features-r1.md). (1) H
- passed=True blocking=0 | Adversarial verification of boundary_smoke report output: ALL FIVE MANDATED CHECKS PASS by execution, zero critical/major findings. (1) CI consistency: recomputed ALL 14 win-share bootstrap CIs (best_
- passed=True blocking=0 | T-boundary round 1: PASS, zero critical/major. (1) FORMULA VS PAPER: re-fetched arXiv:2605.08251 from two sources (arxiv.org/html/2605.08251v1 + ar5iv); paper states Delta_MSE=MSE_noisy-MSE_ZNE (>0 = 
- passed=True blocking=0 | T-mitigation r1 PASS, zero critical/major. All claims verified by RUNNING instrumented code (scripts kept in scratchpad; full evidence in notes\\T-mitigation-r1.md). (1) zne_fr == spike's fixed-Richar
- passed=True blocking=0 | T-experiment shots-axis round 1: ALL assigned contract items verified by execution, zero critical/major defects. (1) CROSS PRODUCT: shots [128,512,2048] x 4 circuits x 2 backends -> exactly 24 rows, e
- passed=True blocking=0 | T-model round 1: PASS on all four mandated areas, verified by running code (scripts in scratchpad; note at E:\quatum  computiiing\qem-selector\notes\tester-t-model-r1.md). (1) significant label: tie/w

## Reviewer findings (4 reviewers)
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Significant-label sigma treats consumed shots as pooled shots — understates sigma for every mitigated technique: derive_significant_label (per-seed route, ~lines 527-567) computes sigma_shot(<tech>_value, <tech>_shots) where <tech>_shots = shots_consumed(tech, base_shots). sqrt((1-x^2)/N) is only valid for one direct estimate from N pooled shots; the techniques do not pool. Verified factors: zne (Richardson (1,2,3), coefficients (3,-3,1), sum c^2=19) sigma understated x7.5; zne_fr ((1,3) equal_split) x2.2; c
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Aggregated-route value proxy '1 - mean_abs_error' is anti-conservative and produced the shipped smoke artifact: The docstring calls the aggregated proxy 'conservative'; it is the opposite. sigma_shot(1-err, N) has variance term ~2*err, so small errors force near-zero sigma regardless of the TRUE expectation (which sets the binomial variance; ideals in the smoke span -0.717..1.0, median 0.29). Verified on results/boundary_smoke/aggregated.csv: winner sigma understated median x3.6, max x54.9; 5/24 (21%) label
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — calibrate=True silently ships an uncalibrated model stamped calibrated:True when a singleton class exists: Reproduced with the venv (sklearn 1.9.0): with one singleton class in y (exactly the research sweep's lone 'zne' win described in model.py's own docstring), _fit_calibrated hits smallest-class-count=1, skips the grouped path, computes cv_fallback=min(5,1)=1<2, and returns the bare RandomForestClassifier. The bundle still records calibrated=True, metrics['calibration'] (brier before/after) is still
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Abstain threshold is honest but unactionable: no OOF selective-accuracy/coverage reported: The threshold is user-supplied, stored verbatim, never tuned on any holdout, and abstain_rate_cv comes from out-of-fold (calibrated when calibrate=True) probabilities — no test leakage; this part passes. But metrics report only the abstain RATE, not accuracy on the retained rows, so there is no honest way to CHOOSE a threshold without peeking at held-out evaluations. Emit coverage vs selective-acc
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\features.py — Ruling: feat_log2_shots is legitimate signal, not leakage — with one required paper caveat: Ruled legitimate on three verified grounds: (1) it is a deployment-time controllable input — recommend() requires base_shots for feature_version-2 bundles, so nothing seen at train time is unavailable at predict time; (2) no fold leakage — the CV group key (family, n_qubits, depth) deliberately excludes base_shots, so the same circuit config at 256 and 4096 shots lands in the SAME fold; (3) shots 
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Post-selection winner bias in the significance margin test: The winner is chosen by argmin over the same noisy per-row errors that feed the winner-vs-runner-up margin test; with 7 techniques the winner's error is selection-biased low, making the test slightly anti-conservative even with corrected sigmas. Second-order once the two high-priority sigma fixes land; a one-line acknowledgment in the paper (or a mildly inflated k_sigma) suffices. Also for the rec
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — Row-level inference ignores the sweep's cluster structure — p-values and CIs are anti-conservative: The research sweep is 180 circuits x 9 backends = 1620 rows, one seed per cell (verified). Rows sharing a circuit are strongly correlated, but paired_permutation_test, bootstrap_ci, and win_share_ci all treat rows as i.i.d. Measured on results/research/results.csv: ICC of the paired (raw_plus - raw) difference by circuit = 0.25; ICC of the 'cdr wins' indicator by circuit = 0.635. Row-level two-sid
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — sigma_shot applied to mitigated estimates with budget-total shots understates sigma ~3–8x, corrupting tie flags and significance labels: winner_margin_below_k_sigma (stats.py) and derive_significant_label (model.py, same helper) compute sigma_shot(value, <tech>_shots), but <tech>_shots is the consumed-budget ledger and the binomial formula only holds for direct measurement. Verified: zne uses RichardsonFactory over scale factors (1,2,3) with zne_shots=12288=3x4096; Richardson coefficients (3,-3,1) give Var = 19*(1-v^2)/4096, i.e. t
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — Percentile-bootstrap win-share CIs collapse to zero width at 0/n shares — [0,0] '95% CIs' already published: win_share_ci's percentile bootstrap of a 0/n (or n/n) proportion returns a zero-width interval with 0% actual coverage. results/boundary_smoke/report.md §8 already ships 'zne 0 [0, 0]', 'zne_fr 0 [0, 0]', 'cdr_ridge 0 [0, 0]' as 95% CIs from n=24 (Wilson 95% for 0/24 is [0, 0.138]); test_win_share_ci_all_wins_is_one even pins the degenerate lo==hi behavior. This will recur precisely where the pape
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — Refusal-conditioning bias: NaN-drop policy conditions cdr comparisons on CDR's easy region; Cliff's delta drops per-array, not pairwise: paired_permutation_test drops pairs where either error is NaN; CDR refuses on 415/1620 rows and refuses on the hard ones (verified: mean raw error 0.544 on dropped rows vs 0.360 on kept), so every cdr-vs-X test is conditional on CDR succeeding — 532 of 1620 pairs silently vanish for cdr_vs_rem. cliffs_delta is worse: it drops NaNs per-array (listwise), so rem's sample retains rows where CDR refuse
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — koester_checklist is not faithful to arXiv:2605.29872 — it implements a different (internal) checklist under the paper's name: Re-fetched the paper (abstract retrieved; full text HTML 404s). Köster & Mauerer propose an eight-criterion review framework (statistical rigour / reproducibility / reporting quality) and minimum reporting standards: explicit parameter documentation, parameter-sensitivity robustness checks (their 132-config sweep), longitudinal drift assessment, and inferential testing with effect-size reporting. 
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — No multiple-comparison correction anywhere, and the top2 contest is post-hoc selected from the same data: Grep confirms no Holm/Bonferroni/BH machinery in src. The current §8 runs 2 tests, but the paper plan implies 7 techniques (21 pairwise contests) across many slices (5 families x 9 backends x shots axis for Angle 3; CDR-variant overfitting map for Angle 2) — dozens of uncorrected p-values. Additionally 'top2_<a>_vs_<b>' picks the two highest win-share techniques from the same data it then tests (s
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\stats.py — inf values bypass the module's stated NaN policy: _clean_1d masks only NaN (np.isnan), not non-finite. Verified: stats.bootstrap_ci([1,2,inf,3]) returns estimate=inf and hi=nan with n_dropped_nan=0 — silently producing NaN output, which the module docstring explicitly forbids; cliffs_delta counts inf-inf pairs as ties (inf-inf -> NaN -> neither greater nor less). Switch to np.isfinite (counting dropped non-finite values) or raise on non-finite in
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\report.py — §8 presentation: overlapping-CI fallacy wording and uninterpreted Cliff's delta: The win-share section states 'A CI that straddles a rival's estimate means the win-share ordering is not resolved' — the overlapping-CI fallacy (overlap does not imply non-significance, and shares are correlated multinomial components; the right object is a CI on the top-2 share difference). The effect-size table prints bare deltas with no magnitude bands (|d|<0.147 negligible / <0.33 small / <0.4
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\mitigation.py — cdr_ridge crippled by alpha-scale artifact — Angle 2 comparison invalid: CDR_RIDGE_ALPHA=1.0 is applied to a single UNSTANDARDIZED feature (noisy expectation in [-1,1]). Over 10 training points Sxx=sum((x-mean)^2) is O(0.01-1), so the Ridge slope Sxy/(Sxx+alpha) is shrunk 43-99% and predictions collapse toward the training-ideal mean. Reproduced: layered_random_q2_d4_s0 @ FakeManilaV2@x0.25 (near-noiseless, true map ~identity): Sxx=0.7645, OLS slope +0.995 -> pred -0.7
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — calibrate=True silently ships an uncalibrated model stamped calibrated:True: _fit_calibrated (lines ~309-362) computes k=min(n_splits, smallest_class, n_groups); the refit call passes the FULL y including CV-dropped singleton classes, so any singleton class (a lone 'raw' win — common) gives smallest=1 -> k=1 -> grouped path skipped, then cv_fallback=min(5,1)=1 -> FrozenEstimator path skipped -> the bare uncalibrated estimator is returned. train_and_eval then stamps bundle[
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Significance-label sigma model wrong for multi-execution techniques; ~10% of research labels flip: derive_significant_label (~lines 527-571) uses sigma_shot(value, <tech>_shots) with TOTAL consumed shots, as if all executions pooled into one average. Extrapolation/correction estimators AMPLIFY variance instead: zne (mitiq Richardson nodes (1,2,3), coeffs (3,-3,1), each level at B shots) has Var=19v/B, not v/(3B) — sigma underestimated 7.5x; zne_fr (equal split, coeffs (1.5,-0.5)) has Var=5v/B, 
- [medium] E:\quatum  computiiing\qem-selector\scripts\make_report.py — Contracted scripts/compute_stats.py never built — report §8 pipeline unrunnable from CLIs: INTERFACES.md V2.8 (~line 243) assigns B6 'scripts/compute_stats.py — NEW CLI --data <csv> --out <dir> writing stats.json'; make_report.py's --stats-json help (line 46) says the file is 'written by scripts/compute_stats.py'; qemsel/stats.py's module docstring documents its exact output schema. The script does not exist — notes/B6-stats.md explicitly declared it out of scope and nobody picked it up
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\model.py — Single-class holdout complement crashes the whole training run: _leave_one_group_out (~line 263) fits with no guard; if holding out one family/backend/device/shot-budget leaves single-class training data, GradientBoostingClassifier raises ValueError ('y contains 1 class...') which propagates out of train_and_eval — no bundle, no metrics written (reproduced with a synthetic df where one backend's complement is single-class). Pre-existing for LOFO/LOBO/LODO but 
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\boundary.py — estimate_params ignores grid_spec eps_feature override — theory axis mismatch when overridden: overlay_selector_vs_theory honors grid_spec['eps_feature'] for the grid's eps axis, but estimate_params hardcodes DEFAULT_EPS_FEATURE ('avg_2q_error') for the secant bias slope (~line 167-179). With an override (e.g. 'avg_readout_error') the D_p/alpha params are derived against the 2q-error coordinate while delta_mse is evaluated on the readout coordinate — an inconsistent boundary curve. Latent t
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\boundary.py — Theory boundary assumes eps-scaling amplification but zne_fr folds gates; readout bias makes the analytic curve quantitatively wrong (sign flip on Lagos): estimate_params derives alpha by secant from (0, mu0) to one noisy probe, so D_p = alpha^2 includes READOUT bias, which fold_global (ZNE_FR_FOLD_METHOD='global' in mitigation.py) never amplifies and Richardson therefore cannot remove. Verified by execution (mirror n2 d4, B=4096, 30 seeds, 200k-shot asymptotics): FakeLagosV2 theory delta_mse = +0.258 (HELP) vs empirical MSE_raw - MSE_zne_fr = -0.00
- [high] E:\quatum  computiiing\qem-selector\configs\boundary.yaml — Overlay selector side is degenerate: full-menu argmax labels give an empty ZNE region (smoke bundle literally has no ZNE class): Theory 'help' means zne_fr beats raw pairwise at equal budget; the overlay's 'selector chooses ZNE' requires zne_fr to beat the whole 7-technique menu including cdr (which wins ~84% of accepted rows). In results/boundary_smoke: zne_fr wins 0/24 menu labels, the trained bundle's classes are ['cdr','cdr_ridge','raw','raw_plus','rem'] so the model cannot emit a ZNE label, zne_vote_share = 0.0 at all 
- [high] E:\quatum  computiiing\qem-selector\src\qemsel\mitigation.py — cdr_ridge with fixed Ridge(alpha=1.0) on one unstandardized feature is over-regularized and inverts the Korolev anchor (worse than raw): With ~10 training points and a single expectation-value feature, Sxx ~ 0.6, so alpha=1.0 shrinks the CDR slope by factor Sxx/(Sxx+1) ~ 0.39. Measured (layered_random n2 d4, FakeManilaV2, 4096 shots): cdr_ridge error 0.288 vs cdr 0.0069 vs RAW 0.079 — 'Ridge-CDR' is 3.6x worse than doing nothing. Smoke dataset means: cdr_ridge 0.1418 vs cdr 0.0211. The Angle-2 experiment as shipped would 'find' reg
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\boundary.py — eps = avg_2q_error is not a sufficient statistic across devices; params estimation mixes observable classes and carries unreported probe noise: Same overlay x-coordinate, opposite empirical regimes: Manila@x1.5 (eps 0.01493, zne_fr helps +0.0136) vs Lagos@x1.0 (eps 0.01463, harms -0.003), because readout/2q ratio is ~3.7 vs ~13.9 — a single boundary curve in (eps, B) cannot represent fold-based zne_fr across both devices. Compounding: Lagos readout is cap-saturated (0.4638 plain -> 0.45 at x1.5/x2.0, non-monotone), the x1.0 point runs com
- [medium] E:\quatum  computiiing\qem-selector\src\qemsel\report.py — Shipped report section 9 and boundary.yaml comments state zne_fr uses @x-dial amplification on sim — it uses gate folding everywhere: configs/boundary.yaml ('zne_fr's @x noise amplification matches the paper's parameter-scaling on sim but not hardware gate-folding') and report section 9 caveat 3 both imply the sim-side zne_fr amplifies noise via the @x dial. The implementation folds gates on sim AND hardware (ZNE_FR_FOLD_METHOD='global' -> fold_global in _apply_zne_fr). The text describes spike option (a); B1 implemented option 
- [medium] E:\quatum  computiiing\qem-selector\configs\boundary.yaml — Dial + shots grid traces the boundary at only ~one budget in the trustworthy region; planned 16384 budget was dropped: Realized eps span 0.00249-0.02927 (12x) and budgets [256,1024,4096] move the q=0 crossing (eps* ~ B^-1/2) by 4x, so the crossing does sweep the dial in principle; verified at B=4096 it sits between Manila x1.0 (delta_mse -0.0016) and x1.5 (+0.0087). But at B=256 the entire Manila dial is theory-harm (smoke confirms no help point), so the help side at low budgets is reachable only through Lagos — e
- [low] E:\quatum  computiiing\qem-selector\src\qemsel\mitigation.py — Verified-good core mechanics worth keeping frozen (plus two low notes): Executed checks confirm: richardson_coefficients((1,3)) = (1.5,-0.5); K = 4*nu (q=0) / 5*nu (q=1) matching Scavino's (1,3) uniform rule; zne_fr equal_split builds exactly 2 executors at base_shots//2 with the passed executor never invoked (SHOT_MULTIPLIER_V2['zne_fr']=1 truthful); guards for all three cdr variants fire before any noisy call. Low notes: (1) raw (executor at B, seed s) and zne_fr le

## Vote results (36 votes, 3 per HIGH finding, >=2 not-refuted = upheld)
- refuted=False | CLAIM REPRODUCES — could not refute; verified by execution. Command: cd "E:\quatum  computiiing\qem-selector" && .venv\Scripts\python.exe <script> where <script> is the repro at C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computiiing\13852342
- refuted=False | CONFIRMED by execution. Repro command: & "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" "C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computiiing\13852342-c2be-4d47-b461-7c0561ff460a\scratchpad\repro_boundary_claim.py" (mirror_
- refuted=False | Claim REPRODUCES essentially digit-for-digit via independent execution (script: scratchpad\voter2_boundary_check.py, run with the project venv python). Mirror n2 d4, ZZ, B=4096, 30 seeds: FakeLagosV2 theory delta_mse = +0.257624 (claim +0.258, HELP) 
- refuted=False | Claim reproduces on every checkable point. (1) results\boundary_smoke\aggregated.csv: best_technique counts = cdr 13, rem 6, raw_plus 3, cdr_ridge 1, raw 1 — zne_fr wins 0/24 (repro: venv python one-liner value_counts on the CSV). (2) metrics.json la
- refuted=False | CONFIRMED, not refuted. Ran the code/artifacts: (1) results/boundary_smoke/model.joblib bundle classes = ['cdr','cdr_ridge','raw','raw_plus','rem'] with label_column='best_technique' — the trained selector structurally cannot emit a ZNE label (matche
- refuted=False | CONFIRMED, could not refute — every number reproduces. Repro: (1) results\boundary_smoke\boundary.json shows zne_vote_share=0.0 at all 15 grid points, selector_help_share=0.0, iou_help=0.0, agreement_pct=86.667 with theory_help_share=0.1333 — so agre
- refuted=False | Claim REPRODUCES exactly, to every quoted decimal. Command: cd "E:\quatum  computiiing\qem-selector" && .venv/Scripts/python.exe with a script that builds circuits.layered_random(2,4,seed=0), pauli "ZZ", make_executor("FakeManilaV2",4096,0), then app
- refuted=False | REPRODUCED EXACTLY, could not refute. Fresh live run (not the stored CSV) of layered_random n2 d4, FakeManilaV2, 4096 shots, seed 0, pauli ZZ via apply_technique: raw abs_error 0.079005, cdr 0.006944, cdr_ridge 0.288248 (3.65x worse than raw) — match
- refuted=False | REPRODUCED EXACTLY, live from current code (not the CSV). Command: "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" running (with src on sys.path): circ=circuits.layered_random(2,4,seed=0); ex=backends.make_executor("FakeManilaV2",4096,
- refuted=False | Claim fully reproduces. (1) model.py derive_significant_label per-seed route (lines 530-532, 565-567) computes sigma_shot(<tech>_value, <tech>_shots), and results.csv's <tech>_shots columns verified equal to base_shots * SHOT_MULTIPLIER_V2[tech] for 
- refuted=False | CLAIM REPRODUCES — NOT REFUTED. Every element verified by running the actual code.

(1) Structural mechanism confirmed: derive_significant_label per-seed route ("E:\quatum  computiiing\qem-selector\src\qemsel\model.py" lines 526-532, 565-567) compute
- refuted=False | Claim reproduces in full. (1) Verified structurally: results.csv <tech>_shots == shots_consumed(tech, base_shots) for all 7 techniques (asserted on the real CSV), and model.py derive_significant_label's per-seed route (lines 526-567) passes those poo
- refuted=False | CLAIM CONFIRMED — could not refute; every quantitative assertion reproduces exactly. Reproducing command: run "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" on the script at C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computii
- refuted=False | Claim fully reproduces. (1) model.py lines 441/534 do call the aggregated proxy 'conservative'. (2) Running derive_significant_label on results/boundary_smoke/aggregated.csv gives label counts {cdr 10, raw_plus 2, rem 3, tie 9} exactly matching shipp
- refuted=False | Claim CONFIRMED by execution, not refuted. Ran the shipped code paths against results/boundary_smoke artifacts (script: scratchpad\skeptic1_agg_proxy.py, venv python). (1) derive_significant_label on aggregated.csv reproduces label counts {cdr:10, ra
- refuted=False | CONFIRMED, not refuted. Reproduced with the venv (sklearn 1.9.0, FrozenEstimator present) via "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" running a script that builds a 17-row df with classes cdr=8, raw=8, zne=1 and calls train_and
- refuted=False | CONFIRMED — claim reproduces exactly with the venv (sklearn 1.9.0). Reproducing command: & "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" "C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computiiing\13852342-c2be-4d47-b461-7c0561f
- refuted=False | CLAIM REPRODUCED, not refuted. Ran train_and_eval(calibrate=True, abstain_threshold=0.6) in the project venv (sklearn 1.9.0, FrozenEstimator present) on a 60-row learnable df with one singleton class (cdr=1, raw=29, zne=30): the persisted bundle's 'm
- refuted=False | Claim CONFIRMED by running the actual code and data. (1) stats.py verifiably has no cluster support: paired_permutation_test flips signs per-row, bootstrap_ci/win_share_ci resample rows i.i.d.; no groups= parameter exists. (2) results/research/result
- refuted=False | Claim fully reproduces. Code inspection of src\qemsel\stats.py confirms paired_permutation_test (per-pair sign flips, line 242), bootstrap_ci (i.i.d. row resampling, line 148), and win_share_ci have no cluster/groups support. Running the venv python 
- refuted=False | Claim reproduces on the actual code and data. (1) results\research\results.csv is exactly 180 circuits x 9 backends = 1620 rows, 1 seed/cell. (2) paired_permutation_test, bootstrap_ci, win_share_ci in src\qemsel\stats.py have no groups/cluster parame
- refuted=False | Claim REPRODUCES. Empirical test with the actual code path (make_executor + apply_technique, research.yaml settings: FakeManilaV2/FakeJakartaV2@x2.0, base_shots=4096): across 100/60 seeds the run-to-run std of the shipped 'zne' estimate is 3.79x / 3.
- refuted=False | Claim CONFIRMED, could not refute. Code reading verifies the premise: experiment.py:1060 stores <tech>_shots = shots_consumed(tech, base_shots) (budget ledger: zne 3x, cdr 11x, rem 3x), and both stats.py winner_margin_below_k_sigma (lines 494-499) an
- refuted=False | CLAIM REPRODUCES (not refuted). Ran Monte Carlo against the real code: for each of N executor seeds built qemsel.backends.make_executor(backend, 4096, seed) and called qemsel.mitigation.apply_technique exactly as experiment.py does, then compared emp
- refuted=False | Claim fully reproduces. (1) Running the venv python with src on path: stats.win_share_ci(["cdr"]*24, "zne", n_boot=10000, ci=0.95, seed=0) returns {'estimate': 0.0, 'lo': 0.0, 'hi': 0.0, 'ci': 0.95, 'n': 24} — a zero-width 95% CI; the n/n case return
- refuted=False | CONFIRMED, not refuted — every element of the claim reproduces on the actual code and shipped artifacts. Reproducing command: run "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" on a script that loads results\boundary_smoke\results.csv
- refuted=False | REPRODUCED. Ran win_share_ci (src\qemsel\stats.py:170-193) directly: a 0/24 label vector returns {'estimate': 0.0, 'lo': 0.0, 'hi': 0.0, 'ci': 0.95, 'n': 24} and 24/24 returns [1.0, 1.0] — zero-width intervals are structural (percentile bootstrap of 
- refuted=False | NOT REFUTED — claim reproduces exactly, live, against current code. Command: & "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" "C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computiiing\13852342-c2be-4d47-b461-7c0561ff460a\scratc
- refuted=False | CONFIRMED — reproduced digit-for-digit by running the actual code. Live replication of _apply_cdr_sklearn (src\qemsel\mitigation.py: CDR_RIDGE_ALPHA=1.0 at line 241, fit/predict at lines 904-909) for layered_random_q2_d4_s0 @ FakeManilaV2@x0.25, shot
- refuted=False | NOT REFUTED — claim reproduces exactly, live, on the current code. Reproducing command: & "E:\quatum  computiiing\qem-selector\.venv\Scripts\python.exe" "C:\Users\ayush\AppData\Local\Temp\claude\E--quatum--computiiing\13852342-c2be-4d47-b461-7c0561ff
- refuted=False | CONFIRMED, not refuted. Reproduced at all three levels. (1) Isolated: _fit_calibrated (src\qemsel\model.py:309-362) with a singleton class returns a bare RandomForestClassifier (k=min(n_splits,1,n_groups)=1 skips the grouped path at line 331; cv_fall
- refuted=False | CLAIM REPRODUCED, not refuted. (1) Code path verified in src/qemsel/model.py: line 1084 passes FULL y (incl. dropped singleton classes) to _fit_calibrated; there smallest=1 -> k=min(n_splits,1,n_groups)=1 skips the grouped path, cv_fallback=min(5,1)=
- refuted=False | CLAIM REPRODUCED, not refuted. (1) Code path verified in src\qemsel\model.py: _fit_calibrated (309-362) computes k=min(n_splits, smallest_class, n_groups); the final-refit call at line 1084 passes FULL y including CV-dropped singletons, so any single
- refuted=False | Claim REPRODUCES on the actual code and data. (1) model.py derive_significant_label (per-seed route, lines 530-532/565-566) feeds sigma_shot(value, <tech>_shots) with TOTAL consumed shots (zne_shots=12288=3x4096 in results.csv), i.e. pooled variance 
- refuted=False | Claim REPRODUCES exactly. Ran the actual derive_significant_label on results/research/results.csv (1620 rows, k=2) via the venv python; my re-implementation of the label loop was asserted byte-identical to the real function before substituting sigmas
- refuted=False | REPRODUCED, every load-bearing number verified by running the code. (1) Mechanism: model.py derive_significant_label per-seed route (lines 530-532, 565-567) computes _safe_sigma(zne_value, zne_shots) with zne_shots = total consumed 3B; ZNE_SCALE_FACT